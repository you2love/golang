# 代码折叠功能说明

## 功能概述

所有代码块现在都支持折叠/展开功能，方便用户浏览和阅读。

## 使用方法

### 单个代码块
- 点击代码块右上角的 **折叠/展开** 按钮

### 快捷键
- **Ctrl/Cmd + K**: 切换所有代码块（折叠/展开）
- **Ctrl/Cmd + Shift + K**: 展开所有代码块

## 实现细节

### 1. code-collapse.js
负责代码块折叠的核心逻辑：
- 自动初始化所有 `<pre>` 代码块
- 添加折叠/展开按钮
- 处理折叠状态切换
- 支持键盘快捷键

### 2. styles.css
包含代码块样式：
- `.code-block`: 代码块容器
- `.code-header`: 代码块头部（包含标题和按钮）
- `.code-toggle`: 折叠按钮
- `.code-content`: 代码内容区域
- `.collapsed`: 折叠状态类

### 3. HTML 页面
所有 HTML 页面都已更新，在底部添加：
```html
<script src="code-collapse.js"></script>
```

## 默认行为

- 页面加载时所有代码块**默认展开**（用户友好）
- 用户可以通过按钮或快捷键折叠不需要的代码块

## 兼容性

- 支持所有现代浏览器
- 不影响没有 JavaScript 的环境（代码仍然可见）
- 与现有样式和功能完全兼容

## 文件清单

已更新的文件：
- `code-collapse.js` - 代码折叠脚本（已更新）
- `styles.css` - 样式文件（已有相关样式）
- `*.html` - 所有 85 个 HTML 页面（已添加脚本引用）

## 测试验证

打开任意 HTML 页面，检查：
1. 所有代码块右上角有折叠按钮
2. 点击按钮可以折叠/展开代码块
3. 快捷键 Ctrl/Cmd + K 可以切换所有代码块
4. 页面加载时所有代码块默认展开
